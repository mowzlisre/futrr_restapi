version: "3.9"

services:
  futrr_api:
    build: .
    container_name: futrr_api
    ports:
      - "8001:8000"
    env_file:
      - .env
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
    restart: always